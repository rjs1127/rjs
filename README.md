## v8.15 — 모바일 문장 이미지 크기 단계·긴 문장 정렬 근본 수정

- 모바일에서 글자 크기 1·2가 동일하게 보이던 현상을 줄이기 위해 1~5 단계 간 실제 크기 차이를 다시 조정함
- 모바일 브라우저의 작은 글자 자동 보정 영향을 줄이도록 문장 미리보기의 text-size-adjust 동작을 명시함
- 긴 문장의 상단/중앙 정렬을 JS overflow 측정으로 판단하던 구조를 제거함
- 문장 텍스트를 별도 내부 요소로 분리하고 CSS flex auto-margin 방식으로 변경함
  - 영역 안에 들어오면 가운데 정렬
  - 영역보다 길어지면 자동으로 첫 줄이 상단에 붙고 아래쪽만 잘림
- 글자 크기 4/5처럼 임계값 근처에서도 비동기 측정 오판으로 위아래가 동시에 잘리는 현상을 방지함
- 글자 크기 기본값 2, 글자 굵기 기본값 보통 및 v8.12의 폰트별 굵기 정책 유지
- 모바일 클립보드 복사 핵심 함수는 변경하지 않음

**커밋 메시지 요약:** `fix: make share preview overflow deterministic on mobile`

---
